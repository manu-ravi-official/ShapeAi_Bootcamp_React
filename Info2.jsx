import React from "react";
function Info2() {
  return (
    <div className="note">
      <h1>Java </h1>
      <p>
        What Does Java Mean? Java is an object-oriented programming language
        that produces software for multiple platforms. When a programmer writes
        a Java application, the compiled code (known as bytecode) runs on most
        operating systems (OS)
      </p>
    </div>
  );
}
export default Info2;
